# NO.1080 并集 造件 spec（下个会话据此造·2026-08-24）

## 🔴 总纲（SS⑤ + veda 收敛·08-24·置顶·A/B 两类锚点都是它的实例）
**结论句不携带它是怎么得来的，所以对与错在阅读时不可区分。** ⇒ 今晚所有真修法只长成两种形状：**要么把「取得方式」写在判据旁边，要么取消那个可以被凭空填写的格子。**
同形对（结论行里对错长一样、全靠"取得方式"区分·**今晚已收 9 对·含但不限于**——本清单若被当穷举而实缺项，恰是本总纲说的那个病·SS 08-24 逮）：① 没试过 vs 试过不行｜② 扫错字的 0 vs 扫对字的 0｜③ range 声称覆盖 vs 真读过｜④ 记录当时对·后来静默过期 vs 记录一直对｜⑤ 抄第一台的数 vs 独立走到同一个数｜⑥ 只测过"报得太多"·没测"报得太少"的闸 vs 已验证（测试名同形·＝判掉 veda `\b` 版那条判据本体）｜⑦ 用 A 捞语料评 B（只得 B-相对-A、测不到 A 自漏）vs 绝对召回率｜⑧ 数据里有坏值 vs 取数路径弄坏好值（三支·加"写入路径当时弄坏"·区分靠 hex()＋未损孪生）｜⑨ 没复发 vs 已修好（计数上同形·零复发证"没复发"不证"已修好"·条件性触发可能只是没被采样）。

**状态**：规格已齐并升 **v0.5**（v0.4 作废）。veda 二次核（不同仪器）出三条改判 + SS 自推翻 S10，SS 回源核成立、Mylos **独立回源核（新半 vault 实体 + 旧半 live 实体，非收结论）全部确认**。造件未开始，属 fork 准备步、今晚核心。B档=走 PR·人签+二签。

## v0.4→v0.5 改判（Mylos 已独立源核·逐位对齐 SS/veda）
1. **S12 与 S11d 同源合并**：旧半 `:211` 一行两句——甲句「唯一人工点＝不可逆决策人一键拍（ESCALATE 给决策清单不给原始 findings）」＝ S12＋S11d 同出一句 ⇒ **并成一格·射程写「全局·不限跨组织」**。乙句「跨 tenant 修复」只有三条＝S11a/b/c。**旧「跨 tenant 四条」是错拆·作废**（v0.1 Mylos 错、veda 交接件同错）。
2. **S11a 改判「部分」**：新半 `:127-129`（`must`+`never`·词 `待核`/`已验`）＋ `:161-164`（`疑似已闭·待核`）已有「存疑标记」纪律的一般形式 ⇒ S11a 非全缺、**只缺跨组织射程**，并入 S8 家族·**沿用新半词汇**（`待核`/`已验`/`疑似已闭·待核`），不另造词。
3. **S10 绿→黄**：新半 `:76` 是 Sync 流程 step 4（射程限 sync）、`:301-313` 是目的地分类表（非要求句）⇒ 均不构成旧半 S10「有人让记住 X 时·写入前先归位」的**要求句**（旧半触发＝日常入口·不限 sync）⇒ **S10 未落地。「待核零」但「已落地」格现在空。** 根因＝判绿看「材料在不在」非「要求句在不在、射程对不对」（本会话第二次·与 #7 同族）。

## 输入（三处备份已到位=不可逆前置已满足）
- **新半**（演进版·为基）：`~/zylos/vault/no1080-provenance-half-20260824/SKILL.md.veda-live-20260824` = 24,303 B / sha256 `30ad5074377a9341bc7025e6643bec9b1116fa1c15934cb46e98d1a9e01eff72`（含 5b/5c/5d/6/7/7b/7c canon 门）— Mylos 台实算指纹一致 ✓
- **旧半**：`~/zylos/.claude/skills/zylos-memory/SKILL.md`（我台 live）= 21,664 B / sha256 `2fb2253620779ec656705ed8ffdb781b54e4a354a88555552ebb0bf798d3db02`（含防复发闸 v2.3 整节 #1-#8 + 节末没编号段落）— Mylos 台实算 ✓
- fork 备份分支 `veda/provenance-half-30ad5074`（commit 47b599f·已标 DO NOT MERGE）

## 🔴 跨行核实测（Mylos 08-24·两半全文逐段·造件蓝图·发现三锚点验收有缺口）
两半各有对方**没有**的承重内容——不是"新半为基+加回几条 S"，是**双向超集合并**：

**仅旧半有（2fb22536·必须加进并集）**：
- `Memory Sync` 段的 **maintenance-only 注记**（sync 子代理禁 C4/禁改业务仓/禁 runtime 改动）
- **in-flight sync 检查**（启动 sync 前查有没有在跑的 sync writer·防双写）
- Sync Flow step 8 的 **8-verify 机械回读闸**（create 后读 DB 回验 id/end_id·"报告也可能是空转的验证器"）
- Sync Flow step 8 的 **8-face 摘要承载闸**（canon `f86f0eb5bd603d12`·取值=并集非只本窗口·每轮重跑·漏写无症状·禁字纳入 summary）
- **## 防复发闸 v2.3 整节**（8 gates + 自运行目标一键拍 + 跨 tenant 三条）
- Consolidation Review / Core File Budgets 里的**防复发闸#1 交叉引用**（do NOT auto-trim always-loaded layer）

**仅新半有（30ad5074·基）**：canon marker 版 5b/5c + **5d 整节**；step 6 富版（(a)-(d) 子闸 + Sync#830 例 + `Never hardcode one machine` + 6e）；step 7 富版（how-to-trim + (a)-(d) + freeze 注 + monotonic writer 注）；**7b 禁字闸 + 7c 客户金额扫**；step 9 的"报实测字节非记忆值"。

**两半都有**：frontmatter/Architecture/base 结构/5b·5c 主体（新半带 canon marker）/Classification/File Formats/Supporting Scripts/Best Practices。

🔴🔴 **验收缺口（我造件前逮到·必报 veda/SS）**：veda 三锚点（`do NOT autonomously trim`/`5d:canon:start`/`Never hardcode one machine`）**盖不住 8-verify/8-face/maintenance-only/in-flight**——这些是旧半独有、且**不在任何一个锚点射程里**。按三锚点造出的并集**可以缺这四样却全绿**（＝又一个"看起来成功的失败态"）。⇒ **三锚点必须补 4 个旧半独有锚**（候选：`8-verify` 或 `机械回读闸`／`8-face` 或 `摘要承载闸`／`maintenance-only`／`in-flight sync`），每个按 veda 判据"先在它该命中的文件验一次"。

## 造法（顺序）
1. **先跨行核**（veda 合成缺口判据）：把旧半的**射程**列出（不是列 8 条编号），逐个问"新半有没有**一处完整落地**"，**部分落地=没落地**。重点扫判"部分/黄"的条。
2. **新半为基**，按下方 v0.5 清单加回。
3. 自声明三词：**"以演进版为基 + 回收未吸收旧闸 + 一条口径待人裁"**（非"合并"非"替代"，写任一都不实）。
4. 独立验收（新造件·第一次运行就在生产）：session-start 注入走一遍 + memory-status.js 跑一遍，**跑过才算·读过不算**。

## 造并集清单（v0.5·取代 v0.4）
🔴 **方法判据（贯穿全表·今天最有用一条）：实例/叙述提及 ≠ 闸，而且误判方向恰好是「绿」。** grep 计数会把 S2/S3/S8/S9/S12 全误判成"已落地"，命中的都是历史举例/轶事/背景/功能说明；**误判方向＝旧闸静默消失那一侧（绿）**，所以不是"不准"、是"往危险侧不准"。判"落地"看该规则的**要求句**在不在、射程对不对，不看有没有提到它。
- **未落地·原样活到并集**：S1 谁有权动常驻层｜S2 动手前备份（:191/:336 是背景+功能说明·非要求）｜S3 第二人（maker|checker 3 处全举例/轶事/peer观察·没一处要求 trim 需第二人·SS 一手核）｜S4 同一实体不两份档案｜S6 写入时用什么形式（摘要+指针/明细/决策条目）｜S8 关键标识符全文一致性（**全文射程**：标识符族 revId/单号/表数/金额口径 + 终版唯一 + 改值时全文 grep 逐处对齐/标 superseded）｜S9 规则要有定时器（:338/:351 是实例·非"SOP 必须挂定时器"）｜**S11b 不按外部推断删改**（infer|推断 零命中）｜**S11c 跨组织授权**（tenant|cross-org 零命中）
- **S11a/S12+S11d（v0.5 改）**：
  · **S11a＝部分**：存疑标待确认——新半已有一般形式（5c(b):127-129 / 5d:161-164）·**只补跨组织射程·沿用新半词汇**·并入 S8 家族不重复造格
  · **S12＋S11d＝一格·射程「全局·不限跨组织」**：不可逆改动唯一人工点＝决策人一键拍 且 ESCALATE 给「改啥+为啥+建议」决策清单、非原始 findings（旧半 :211 甲句）
- **部分·按反例补齐**：S5 跨文件双源收敛｜S7 同主题多版本周期性全库扫（5c 只覆盖本窗口·跨 checkpoint 边界旧版扫不到）
- **黄（v0.5 新·原绿推翻）**：S10 写入前归位（分类+哪个文件）——新半 :76 限 sync 流程、:301-313 只是目的地表·**均非入口要求句 ⇒ 未落地·原样活到并集**
- **已落地**：（空——S10 推翻后无绿格）
- **待核：零**
- **反向保住·新半独有（并集不能丢）**：见下节
- checker 件（最新）：SS 同容器追加 **v0.5 = 29,328 B / sha16 `0e05c293303b8e4a`**（追加前 v0.4 = 21,163 B / `e24d46c468aba6a4`）

## 逐条全文射程（造并集时执行）
- **#1 加三前置**：新门 6(a-d)/:177/:226 缺 备份（6c git log -p 是事后验证非动手前备份）/maker≠checker（6a-d 单人无第二人）/决策人一键拍（只 freeze 时回人）⇒ 三前置加回新门·不裁口径。剩"无 freeze 时 sync 能否自动 trim"待三前置加回后看还剩不剩（可能才需波总）。
- **#8 按全文射程加**：标识符族 + 终版唯一 + 改 canonical 值全文 grep 逐处对齐/标 superseded。7b 只覆盖一个码点 U+710A ⇒ 射程缩减·红判硬。
- **乙句（跨 tenant）按三条加回**（没编号段落·逐句重扫·找回一条≠那段闭）：① 存疑标"待确认"（＝S11a·并入 S8 家族）② 不按外部推断删改（S11b）③ 先取对方组织 owner 授权·组织边界>技术可行（S11c）。
- **甲句（全局·非跨组织）单列一格**：决策人一键拍 + ESCALATE 给决策清单不给原始 findings（S12+S11d）。
- **C1（验收项·v0.5 新增）**：加 S12 **必须改写新半 `:176-177`/`:225-227` 那两句本身**——「trim until it is back under」是无条件命令式，尾部追加闸拦不住（:228-232 已有"怎么 trim·别删限定词"块·但只管 HOW 不管 WHO 授权）。

## 🔴 造件次序约束（三条·不需人裁·但不能颠倒·SS BP 下游穷举 + Mylos 回源核）
BP 节（新半 `:398-406`）有几条看似弱复述，删前必须先核「有没有下游在消费」——两条承重、失效都是静默的（下游读到空、照常报成功）：
```
① 提升 :402（explicit dates/timestamps）成要求句  → 然后才可动 BP 那节
   （承重：下游 :380「Review entries by their dates and status fields」·veda 已二签）
② S10 以要求句落地                                → 然后才可删 :404
   （承重：下游 Sync Flow step3 :75「Read memory files(…user profiles…)」·不归位则步3读空档案·sync 静默照走）
③ 落位闸先在 / S8(闸8) 最后加                     （前几轮已定）
```
🔴 **不变量（不是次序·是"不许动"·与三条并列·SS 补）：`:311-313` 是 `:405` 下游的唯一承担者。删 `:405` 之后，`:311-313` 不得再以"与 BP 重复"为由清理。** 否则步骤6 `:175`「a pointer to the config file」失去保证——又一次静默失效。
判据：**「A 的下游已由 B 承担」这句话会把 B 变成新的承重件，而 B 变承重这件事只记录在做判断者的那条消息里 ⇒ 每次说"已由 X 承担"，就要同时给 X 挂一条"不许动"。**
- BP 六条下游判定（Mylos 回源核 :404/:402/:405 三处成立）：`:400` lean 无下游｜`:401` prefer-updates 无下游（=S4/S5 影子·一条影子两格·两格都落再删）｜`:402` **承重**（→:380）｜`:403` archive 无独立下游（:394-395 自带找回句·并入人裁项①）｜`:404` **承重**（→:75）｜`:405` config 下游已由 :311-313 要求句承担（前提 :311-313 不动）。

## 保住新半独有闸（并集不能丢）
- **6(b)**：「移走并留指针」是两个独立断言·目标读不到只能报 destination unverified·never PASS（:196-201）
- **6(d)**：warn 线不授权删除禁令内容·冲突时越线（:206-211）
- **6(e)·v0.5 新增**：禁把单机字节/warn 线/freeze ticket id 写死进本共享件（:212-220·并集是多台跑的共享件）。`:96-102` 假 drift 两半都有 ⇒ 不列。

## 递波总证据（缺 S8 已造成一次假报）
新半门限五处（`:176` 8KB warn ✓已核 /`:225` 10KB warn ✓已核 /`:357` 硬 16KB /`:365`/`:370`）无机制要求同动，且硬 16KB 与 warn 8KB 同文不标区别 ⇒ Sync#954 把 warn 当硬预算算出 206%/525% 与同报告 103% 自相矛盾，根因＝缺 S8「全文一致性核」。**后三处行号（:357/:365/:370）造并集时同核**（本次仅核到 :176/:225 两处 warn）。

## 🔴 并集完整性验收·N-锚点（veda+SS+Mylos 三方 08-24 收敛·造件必过）
**判据（集合层·SS 立）**：验收集覆盖的东西若比声称装入的少，"没被验的"与"验过且在"结果都是 PASS ⇒ **并集里每个被声称装入的成分都要有自己的锚点·装 N 样就 N 个锚·不是三个**。
🔴 **`do NOT autonomously trim`(:156/:166 trim 禁令) 与 `## 防复发闸 v2.3`(:198) 是两个块、隔约 40 行**——只搬 trim 禁令锚点得 2、判 PASS，而整个 v2.3 八道闸一字没有（veda 逮）。⇒ 一个成分一个锚不够，**段头+末条都要锚**（中间截断才可见）。

| 锚点串（制品内容·非分析标签） | 验什么 | 来源半 |
|---|---|---|
| `do NOT autonomously trim` | trim 禁令块 | 旧半 |
| `Anti-Recurrence Gates · v2.3` | 防复发闸段**头在** | 旧半 |
| `关键标识符全文一致性核` | 防复发闸**末条**（第 8 条·段完整到尾） | 旧半 |
| `8-verify` / `机械回读闸` | 8-verify 闸 | 旧半 |
| `8-face` / `摘要承载闸`（canon `f86f0eb5bd603d12`） | 8-face 闸 | 旧半 |
| `maintenance-only` | sync 子代理禁 C4/禁改仓 | 旧半 |
| `in-flight sync` | 双写检查 | 旧半 |
| 跨 tenant 三条（`存疑标"待确认"`/`不按外部推断删改`/`对方组织 owner 授权`） | 自运行目标+跨 tenant | 旧半 |
| `5d:canon:start` | 5d canon（区间 sha：**权威=`0232dd94d81cd0ff`/1,242B（canon-slice v1 `.strip()` 口径）**；本件非-strip=`1c94eef9369181dd`/1,243B（本件专属值·勿跨件比）） | 新半 |
| `^7b\.` / `Banned-character gate` | 7b 禁字闸 | 新半 |
| `^7c\.` / `Resident-tier customer-money` | 7c 客户金额扫 | 新半 |
| `Never hardcode one machine` | 新半 step6 富版/provenance | 新半 |
| 🔴 裸 `provenance` | ❌**禁用作判据**（宽·污染·单调增长·恒假 PASS） | — |

- 🔴 **"S1-S12"是 union 分析标签、制品里 0 命中**（制品是"每次 sync 跑这 8 道闸"·条目 1.-8.·引用写"防复发闸#1"）——**验收/造件语言里勿用 S1-S12 描述 v2.3，用制品串**；S-标签只在映射表里当分析索引。
- 🔴 **射程钉制品（三方签·grep -r 禁用）**：自验输入必须是**指名单文件路径或 blob sha**；**先校 sha 再 grep**（sha 不对就停）；`grep -r` 在这套检查里**禁用**（对目录树跑命中的是"谁提过"不是"谁含有"——这五+锚点串现已全散落在今晚会话日志/current.md 里·三台实测 current.md 满分假 PASS）。
- 别擦洗假命中（5c(c) 零删除·擦洗是错杠杆·射程收窄一次永久有效）。
- 待 veda 确认：新半 live 30ad5074 上 `^7b\.`/`^7c\.` 命中（我读新半确有 7b:254/7c:289·待 veda 一手复核数）。
- **阳性对照**：拿上游原件跑所有锚点必须 **全 0**（三方实测原件全 0 ⇒ 查法是活的·锚点非恒真）。
- 🔴🔴 **锚点分两类（SS 08-24·推翻 veda⑤"5b/5c 风险低不加锚"）**：veda 评估的是"升级会不会抹掉"，但**验收表验的是那个还不存在的并集制品、失效模式是拼装出错**（基底选错/splice 切歪/粘贴截断/去重去多）——"三台都有"对拼装错误零保护。**方向反了：七个 A 类锚点每个只存在某一半，`5b`/`5c` 是唯一同时存在两半的成分 ⇒ 拼装时唯一会触发"这里重复了、去掉一份"判断的东西 ⇒ 恰好坐在接缝上；让它"低风险"的属性（到处都有）正是让它进入去重决策的属性**。去重出错的产物＝七锚点全 PASS、5b/5c 没了。
  - **A 类·半存在性检查（上表 N 个·字符串锚点·会腐化·需射程钉制品）**：验"某一半装进来没有"，判据 **≥1**。
  - 🔴 **B 类·接缝检查（veda 08-24 修 SS 版·改结构式·不用字符串锚点）**：接缝**不是 5b/5c 两条、是 215 行**（veda 归一化实测：p6 唯一非空行 279 / veda live 369 / 上游原件 167 ⇒ 两半共有 215 行·其中**不来自上游原件的 54 行**＝两半各自打过的相同补丁、**不在 base、去重去多了无从恢复**）。靠列举字符串锚点覆盖接缝走不通（手点 2、实际 215）。改两条结构检查：
    - 🔴 **B1（挡"没去重"·=2 侧·差分版·SS+veda 08-24 收敛定稿·勿用绝对版·勿用旧「加215」⚠️版）**：**union 的重复行多重集必须 ⊆ 各输入各自重复行多重集之并（子集·不是相等）**——判"**有没有新增的重复**"、不判"有没有重复"。绝对版（`sort|uniq -d` 必须为空）是**恒红闸**：p6 自身归一化后就继承 2 行重复（`---` 分隔线 + ` rule-violating entry is allowed only if trivially safe.`，SS live 同两行），完美并集也会 FAIL ⇒ 第一次跑报假错 ⇒ 三天内被加白名单/关掉＝等于没闸（同 morita "弱命中恒真⇒闸恒红⇒被绕过"族）。差分版不引入新定义、正好只挡拼装错误。
      ⚠️ **实现细节（四条·否则仍恒红或失明）**：
      · **基线取值＝各输入内部重复之并（本例＝2 种：`---` + ` rule-violating entry is allowed only if trivially safe.`），逐输入实算·不假设"都一样"或用某份代表**（p6 2 种、veda live/原件各 1 种·veda+SS 两台各自实测）。
      · 🔴 **禁把那 215 条共有行加进基线（SS 08-24 撤回自己上一条 ⚠️·「215 须一并算入预期内重复」整句作废·勿写进 spec）**：215 条只在"没去重"时才重复，而"两半都保留、根本没去重"正是要报的错（情形 A `cat 两半|归一化|sort|uniq -d`=216 ⊄ 基线 2 ⇒ FAIL·正该抓；两台实测超出 214 行）。把 215 加进基线 ⇒ 情形 A 超出从 214 掉到 0 ⇒ 该失效模式当场失明。判据：**把"错误状态下才会出现的现象"写进预期基线＝把该错误从检出集里删掉；基线只能取"在正确产物上也必然出现"的东西**——内部重复符合（继承自输入）·215 共有行不符合（正确去重后各只剩一份）。
      · **必须 ⊆ 不能 =**：情形 B（对并集＝新半/veda live 为基 + 只补 p6 独有 64 行）实测只有 1 种重复、基线 2 种（`rule-violating…` 那条去重后无第二份）⇒ 等号会把正确并集判红（SS+veda 两台实测 PASS）。
      · **基线每次现算·不缓存成常量**：输入本身随每次升级变·写死基线常量正是今晚反复抓的"落盘那刻对、之后静默变错"那类（B 类结构检查的优点就是不含字符串常量·别在基线这里又塞一个）。
    - **B2（挡"去多了"·=0 侧）**：先把 `comm -12`（两半共有）那 **215 行落盘成 `seam.txt` 交件**，union 必须**逐行覆盖**；`comm -23 seam 原件` 那 **54 行设硬失败项**（不在 base、去掉即不可恢复）。
    - 合起来＝"恰好一份"语义、覆盖 215 行。
  - 🔴 **A 类/B 类分开标保质期**：A 类字符串锚点会被自己的讨论污染、随谁写散文腐化、需射程钉制品；**B 类结构式不含任何字符串常量 ⇒ 没有锚点可污染、不腐化**（今晚一个多小时的锚点污染族，结构式检查根本没这个面）。⇒ A 类靠人想想不全（今晚逮出三批）、**更该有 B 类兜底**。
  - ⚠️ **归一化定义写死进脚本**（去空行/压连续空白/`sort -u`）·换定义换数、别各自理解（SS 敏感性测试：p6 唯一非空行 279 在三种归一化定义下同得 279·至少此输入不随定义漂）。
  - **215/54/64/154 现＝veda+SS 两台各自持字节独立复算**（SS 更正：本机有 curl+PAT，纯读 `GET /contents?ref=<branch> Accept:raw` 从 fork 拉两分支字节 + `main` 拉上游原件 `6b42a02385b4e287…`/9943B，三份输入齐、六数全复出：p6 279/veda 369/原件 167/共有 215/致命 54/独有 64·154，阳性对照三锚点原件 0/0/0）。三输入 sha：旧半 `2fb22536`(fork ss/provenance-half-2fb22536 回读 21664B) / 新半 `30ad5074`(veda/provenance-half-30ad5074 回读 24303B) / 原件 `6b42a023`(main·9943B)。
  - 判据两条（今晚 SS 自纠出）：① **"我这台做不到"是可实测的断言、SS 却先用推理代替实测报了"不可验"**（"没试过"和"试过不行"结论句里同形）——试了就全复出；② **只读权限足够做交叉验证**（纯读 GET·零写·推分支仍归 yueran 台·不为"能核"要写权限）。
  - **成员逐个列举、勿写区间**：实际是 `5b / 5c / 5d / 7b / 7c`——`6a/6b/7a` 从不存在（veda+SS 两台实测 0/0），写"5b–7c"会凭空断言三个从未存在的成员、缺席看着像真缺失。
- 🔴 **veda 七锚点之外我跨行核另找到 4 个旧半独有、同样无锚**：`8-verify`(机械回读闸)／`8-face`(摘要承载闸)／`maintenance-only`／`in-flight sync`——它们在 Sync Flow/Memory Sync 段、不在防复发闸段，故 `Anti-Recurrence Gates·v2.3`+`关键标识符全文一致性核` 两个 v2.3 锚**盖不住**。上表已含。⇒ 合并后锚点集 ＝ veda 七 ∪ 我这 4 ∪ 跨 tenant 三条。
- 🔴 **污染锚点警告**：裸 `provenance` 在旧半 p6 里有 1 处命中（`:100` "provenance copy of 5b/5c"·与 veda 半无关）⇒ 谁用 `grep -c provenance>0` 当"veda 半在不在"判据、**光拿 p6 就能过**（p6 里 veda 半一个字没有）＝**不完整并集拿到完整证明**（假 1）。判据：一直防"扫错字假 0"、这次是"锚点太宽假 1"，**0 和 1 两头都得配对照**。
- 🔴 **候选锚点必须先在"它应该命中的那个文件"上验一次**（veda 踩坑：从 wrap 正文读出的 `raise the EXIT flag instead of trimming` 在它自己源文件里也是 0·换行切断了）——阳性对照通常指"证明扫描器会响"，这次反过来是"锚点在自己家里不响"，两个方向都测、只测一头会挑出一个永远沉默的锚点。
- 组成实证：旧半 p6 `do NOT autonomously trim`=2 / `5d.`=0（无 5d）；新半 `5d:canon`=1 / `do NOT autonomously trim`=0（无 v2.3）⇒ **没有任何一台 durable 同时有两半、完整规则集在 fleet 不存在、造件是唯一创造它的动作**。canon 区 **1,242B/`0232dd94d81cd0ff`（canon-slice v1 `.strip()` 权威口径·2026-08-25 实跑复现 5d=1242/0232dd94）**〔**本件**非-strip=1,243B/`1c94eef9`（=本件 region 两端共 1 空白字节；**此 1243 是本件专属值、非通用"非-strip 口径"数·勿拿去比别的文件**，否则再造一次同型假 drift——正是本改动要消灭的·veda ■4+精度补）·值必与口径成对写〕（veda+SS 各自 `sha256sum -c` 对各自 p6 blob=OK·三台各自 grep 同结果·可跨台验第二数）。
- 🔴 **裸 `provenance` 入"禁用作判据"名单（SS 08-24·理由必附）**：宽锚点假命中数**不是常量·随任何人写散文而单调增长**（SS live 已 2 处：`:100` + `:169` SS 21:04 写的台内元信息带 `provenance`），且变坏方向**恒为假 PASS**——写的人不知道自己在给一个判据投票。⇒ 三锚点**全部用"只可能出现在该半正文里"的长串**、禁用任何宽词。
- 🔴 **双向对照判据（veda③ + SS #187 第 5 条·同一枚硬币两面·合成一组）**：一个"永远沉默的锚点"与"这里确实没有"的正确结论**输出完全一样**；"查不到"与"不存在"同形。⇒ **任何判据，0 那侧和 1 那侧都必须各有一次对照**（1 侧＝锚点在它该命中的文件里确实响；0 侧＝阳性对照上游原件 0/0/0 + 污染锚点在不该有的地方不响）。

## checker / 验收（SS 三格）
① 旧半每条要么原样在并集、要么映射表里有一条 SS 核过的吸收关系（整体断言不接受·只逐条）② 无未标注新行（对两输入行级 diff）③ 输入指纹钉（旧半 21664/2fb22536 Mylos 台实算✓·新半 24303/30ad5074 Mylos 台实算✓）④ 落地真跑一次 ⑤ 并集自声明三词·没声明退回。
- checker 件：SS v0.5 = 29,328 B / `0e05c293303b8e4a`

## 落 decisions.md 的判据（本轮）
实例/叙述提及 ≠ 闸·**且误判方向恒为「绿」＝旧闸静默消失侧**（grep 计数误判 S2/S3/S8/S9/S12）｜判绿看「材料在不在」非「要求句在不在·射程对不对」（本会话第二次·与 #7 同族）｜**「有没有更强同类」和「有没有下游在消费」是两个独立问题·而清理动作只问前者**——弱复述的「弱」是相对写它的人、不是相对读它的机制·删任何看似复述的规则前两个问题都要问·且承重项失效多是静默（下游读空照常报成功·正因静默才一直被当弱复述留着没人管）｜合成缺口（射程被两条各半·逐条判部分·拼起来零）｜找回一条≠那段闭·按段落逐句重扫｜递 owner 问题前提须全文读出（截断冲突伪装成授权问题）｜两份同名件"读了全文"须说哪一份｜方法多样性≠只主体多样性｜在场≠被读到（规则放动作发生那一行）｜免检通道没闸｜grep -c 计行非出现次数（用 grep -oF|wc -l）｜成功回执≠内容对（write/取件都要 read-back）

## 归属（落我台 decisions.md 时标）
方法多样性 / 规则放动作行 = veda 台立 → 标"执行用副本·非 canonical"；在场≠被读到 / 误判方向恒为绿 = Mylos+SS 台立 = canonical
